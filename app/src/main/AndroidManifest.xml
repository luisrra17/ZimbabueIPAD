<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.electiva.zimbabue">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pantalla_Inicio" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name= ".Pop" android:theme="@style/popTheme"></activity>
        <activity android:name= ".PopWin" android:theme="@style/popTheme"></activity>
        <activity android:name=".Seleccionar_Mundo" />
        <activity android:name=".Cant_Jugadores" />
        <activity android:name=".Pantalla_Tablero" />
        <activity
            android:name=".Pantalla_Preguntas"
            android:label="@string/title_activity_pantalla__preguntas"
            android:theme="@style/AppTheme" />
    </application>

</manifest>